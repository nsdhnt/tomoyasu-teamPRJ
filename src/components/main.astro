---
import MainImgSlide from "./mainImgSlide.astro";
import { Image } from "astro:assets";
import mainImgDrinking from "../assets/main-img-drinking.png";
import mainImgEating from "../assets/main-img-eating.png";
import mainImgPasting from "../assets/main-img-pasting.png";
import exploreArrow from "../assets/explore-arrow.png";
---

<style lang="scss">
  @keyframes slideWindow {
    0%, 30%{
      transform: translateX(0);
    }
    35%, 65%{
      transform: translateX(-393px);
    }
    70%, 100%{
      transform: translateX(-786px);
    }
  }

  .main {
    width: 393px;
    margin-inline: auto;
    background-repeat: repeat-y;
    .main-img-wrap {
      padding: 96px 0;
      overflow-x: hidden;
      overflow: scroll;
      display: flex;
      position: relative;
      &::-webkit-scrollbar {
        display: none;
      }
      .main-img {
        display: flex;
        animation: slideWindow 15s ease-in-out infinite;
      }
    }
    .explore {
      display: flex;
      flex-direction: column;
      align-items: center;
      p {
        &:first-child {
          font-size: 48px;
        }
        &:nth-child(2) {
          font-size: 24px;
        }
      }
      img {
        margin: 10px 0;
        width: 22px;
      }
    }
    .modal-window {
      &.hidden {
        display: none;
      }
    }
  }
</style>

<main id="top">
  <div class="main">
    <div class="main-img-wrap">
      <ul class="main-img">
        <MainImgSlide src={mainImgDrinking} />
        <MainImgSlide src={mainImgEating} />
        <MainImgSlide src={mainImgPasting} />
      </ul>
    </div>
    <div class="explore">
      <p>colors Taiwan</p>
      <p>explore</p>
      <Image src={exploreArrow} alt="" />
    </div>
  </div>
</main>